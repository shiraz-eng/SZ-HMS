# Shared with packages/database and the portal
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/szhms?schema=public"

NEXT_PUBLIC_ROOT_DOMAIN=szhms.com
NEXT_PUBLIC_MARKETING_URL=http://localhost:3000
NEXT_PUBLIC_PORTAL_URL=http://localhost:3001

# Stripe — test-mode keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
# From `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Recurring Price ids (test mode) for each plan
STRIPE_PRICE_BASIC=price_xxx
STRIPE_PRICE_POLYCLINIC=price_xxx
STRIPE_PRICE_ENTERPRISE=price_xxx
